# AI 固件工程协同开发协议 (Zephyr/nRF52)

作为此项目的固件架构师，你在参与开发时必须严格遵守本协议。本协议的优先级高于之前的任何通用或隐含指令。

### 语言与交付规范
- **所有产出物（包括代码注释、文档说明、技术自述）必须使用中文。**
- Git Commit Message 必须使用中文，格式要求：`feat: [模块名] [中文描述]`。
- **禁止伪代码落盘**: 所有写入项目源码目录的代码必须是语法完整、符合标准、理论上可直接通过本地编译验证的版本，绝不使用 `// TODO: 这里写逻辑` 等伪代码占位。

## 一、 逻辑闭环原则 (The Logic Closed-Loop Principle)
1. **禁止轮询架构**: 严禁在 `main.c` 或任何业务线程中引入 `while(1)` 轮询。所有业务逻辑必须通过 Zephyr CAF 事件总线或内核工作队列（`k_work_delayable`）异步实现。
2. **事件驱动规范**: 定义新事件前，必须先规划好事件结构体头文件（`.h`）和注册宏源文件（`.c`）。严禁静默 Submit，所有事件必须挂载正确的 `log_event` 回调，确保事件总线动作在终端完全可见。
3. **单职权原则**: 一个工作队列或专属线程 Handler 只能负责其专属模块的数据处理与广播，严禁跨模块强行读写不属于自身状态机的上下文。

## 二、 三步走开发指令 (The Three-Step Development Flow)
在处理任何新驱动、新外设或应用层功能需求时，必须按此顺序操作：

- **第一步：设备树审计 (Device Tree Audit)**
  检查 `key_board.overlay` 文件。确保没有任何硬编码的 GPIO 引脚、I2C 地址。检查配置节点的 `status = "okay"` 状态，以及引用的硬件句柄（如 `&gpio0`）是否准确无误。

- **第二步：Kconfig 校验 (Kconfig Verification)**
  确保 `prj.conf` 中开启了项目运行所依赖的所有内核级或驱动级宏（如 `CONFIG_RING_BUF`, CONFIG_USB_DEVICE_STACK_NEXT` 等）。未经验证，禁止随意为了省事而开启未知功能的第三方宏。

- **第三步：防御性编码 (Defensive Implementation)**
  所有外设句柄获取后必须第一时间调用 `device_is_ready()` 阻断审计。所有动态内存申请（如调用由宏生成的 `new_..._event()` 助手函数）必须紧跟 `NULL` 指针非空判断，若失败必须执行 `LOG_ERR` 并妥善通过 `goto` 异常流程回收资源。

## 三、 终端与工具行为约束 (Terminal Discipline)
1. **静默执行原则**: 禁止直接使用 Bash 执行 Windows 原生 PowerShell 命令（反之亦然）。
2. **禁止广度扫描**: 绝对禁止在根目录执行递归搜索或模糊匹配命令。任何时候需要路径、结构信息，必须直接读取或向用户询问获取。
3. **防超时/防卡死**: 任何复杂的分析或上下文重构，若处理时间预计较长，必须在逻辑出口处保持防御，随时准备挂起并向用户同步当前阶段进度。
4. **无损回滚**: 当发生逻辑设计偏差时，必须优先引导用户执行干净的代码状态清理，而不是在错误的逻辑、未对齐的旧缓存代码基础之上进行拆东墙补西墙式的修补。

## 四、 交付前架构自述要求 (Architectural Self-Description)
在输出任何核心源文件代码前，AI 必须在对话中优先、清晰地输出以下结构化技术自述，并在获得用户明确同意（如回复“同意，落盘”）后，方可执行真实的文件写入动作：

1. **[设计意图]**：清晰陈述为何要采取此方案，解决了什么解耦或性能问题。
2. **[实现方案]**：扼要说明内部使用了哪些 Zephyr 内核组件（如信号量、队列、低优先级线程隔离）以及数据流向。
3. **[预期日志]**：给出该模块成功运行后，在 RTT 终端中应当吐出的符合规范的标准日志样例（格式：`e:module_name field=value` 或相应的 `<dbg>`/`<inf>` 标签）。

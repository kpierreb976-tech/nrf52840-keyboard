# ==============================================================================
# 调试叠加配置 — 仅在开发/调试时使用
# 使用方式: west build -- -DEXTRA_CONF_FILE=prj_debug.conf
#
# 按任务只打开当前调试模块，不要全开。
# ==============================================================================

# --- USB 传输层 (当前正在调试) ---
# CONFIG_USB_TRANSPORT_LOG_LEVEL_DBG=y

# --- BLE 传输层 (已稳定，需要时取消注释) ---
# CONFIG_BLE_TRANSPORT_LOG_LEVEL_DBG=y

# --- 键盘核心 ---
# CONFIG_KEYBOARD_CORE_LOG_LEVEL_DBG=y

# --- 电源管理 ---
# CONFIG_POWER_MGMT_LOG_LEVEL_DBG=y

# --- 档位检测 ---
# CONFIG_MODE_SWITCH_LOG_LEVEL_DBG=y

# --- 编码器 ---
# CONFIG_ENCODER_LOG_LEVEL_DBG=y
# CONFIG_ENCODER_MAPPER_LOG_LEVEL_DBG=y
